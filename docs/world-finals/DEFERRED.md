# Deferred — world finals

Every inconsistency, rough edge and shortcut found while building Continuity 2.0 that was
**not** resolved at the time. Nothing here is a surprise; everything here is a decision.

Same format and the same rules as [`docs/DEFERRED.md`](../DEFERRED.md), which covers the
preliminary build and is still live. This file is scoped to the finals work so the two do not
have to be read together. Items are removed when fixed.

**Severity:** 🔴 would be wrong on stage · 🟡 wrong but survivable · ⚪ cosmetic or future ·
🔵 researched and deferred by decision — the direction is settled, a prerequisite is not

**Audited against the code on 10 Sep.** Seventeen rows described things that items 19 to 30
have since built and are now below with what fixed each, including every 🔴 from the 8
September pass. Twenty duplicated rows were removed the same day — my own edits had written
ten of them three times, because the anchor I replaced against is the table header and it
appears in all three sections.

**The third pass through the built product, 10 Sep**, added the eighteen rows at the top of the
live table, and an audit of this file against [SCENARIO-B.md](SCENARIO-B.md) added the two 🔴
about the cross-team response, which is the assigned problem statement itself. Every one was checked against the code before it was written down, and the seven
that needed research or a decision carry a pointer into
[RESEARCH-3rd-Passthrough.md](RESEARCH-3rd-Passthrough.md), which holds what the code does,
the options, and what other people have published about the same problem.

**Phase 8 closed ten of these on 10 Sep**, including all three cross-team 🔴 and two rows
the 8 September flow pass had left standing. BUILD item 28, the board evidence rows and the
EOL waiver then closed with browser evidence, and so did the cross-manufacturer resolution
that was writing one company's part number against another company's name and stock. The
live table then held 16 🟡, 25 ⚪ and 2 🔵.

**Pass 2 ran on 11 Sep**, planned in [tasks/DEFERRED-PASS-2.md](tasks/DEFERRED-PASS-2.md).
P1 to P5, P10 and P11 landed: the local database is the default, the three never-checked rules
are real checks and `not_assessed` is deleted from the vocabulary, every affected line states
an annual volume, a worked precedent is read across lines, what the review skipped is stored,
the graph legend is derived from the states on the board, and the catalogue normalises a
bounded batch concurrently. **P6, P7 and P8 are not started** — the notice-level replay, the
matrix taking what the review resolved, and the board consequence stored asynchronously. **P9 landed the same evening** — see its resolved row.

**No red.** Counted on 11 Sep rather than adjusted, after moving P2's, P5's, P10's, P11's, the source-line row's and the
mailbox pair below:
the live table holds **7 🟡, 22 ⚪ and 2 🔵**, plus 2 🟡 and 11 ⚪ from the first flow
pass; **93 rows are resolved** below with their reasoning.

**One row is new this pass and it did not come from using the product**: a recorded reading's
datasheet sentence is stored and never shown, so the derivations PARTS.md promises are
auditable in that document and not on screen. It was found by looking for the class after the
`signal_integrity` row closed — the same hunt that found the `esr_*` fields dropped by the
same mechanism. The credential incident closed the same evening, and its resolved row keeps
the correction that no database password was ever in the transcript.

| | Item | Where |
|---|---|---|
| ⚪ | **The board's mounting condition is the one operand of the thermal arithmetic with no source line.** `rules.py:904` emits `Evidence(subject, "board mounting", requirements.mounting)` with no source, where the ambient on the same row carries one, so on `/matrix` and in the change request every evidence row reads *value — where it came from* except this one. It renders as a bare value rather than as an admission, which is why it is ⚪ and not the 🟡 it sits beside: the component asks `row.source ? \` — ${row.source}\` : ''`. `OperatingProfile` has no `mounting_source`, and `build_quantity_source` is the pattern to copy — one field, four places in `profile.py`, one in `Requirements`, one in the rule, and a seeded sentence per product line. | `engine/rules.py:904` · `profile.py` |
| 🟡 | **The board pane places a picture per candidate as a stored review replays, and the one that lands last is not necessarily the one the review settled on.** `line.tsx` derives `placing` from a fallback chain — `review.proposal ?? review.trying ?? …` — and `trying` walks every candidate as the frames arrive, so a page opened on a reviewed product starts a KiCad run for each. Each one ends with `setOutcome(await …)`, so whichever *resolves* last wins rather than whichever was asked for last, and an intermediate candidate can be the picture on screen. Found while chasing a browser test that failed on a caption which had been there a moment earlier. The spec now waits for the change-request button, which only renders once the review is no longer running, and gives the caption a real KiCad run's worth of time rather than an interval that was enough when one placement happened. **Two halves are fixed, 11 Sep.** A superseded placement no longer paints — whichever response *arrived* last used to win rather than whichever was *asked for* last, so an intermediate candidate could be the picture on a board the review settled elsewhere — and the pane no longer clears the picture it has to show a spinner over it, which is why a board mid-re-place used to go blank, caption and crops with it. **What remains is the churn**: several placements where one would do, and contending KiCad runs, which is what that browser test is really waiting on. Ending it means not chasing `trying` while a *replay* runs, and the client cannot tell a replay from a live run today — that is a change to what the review's own status means, so a decision rather than a repair. | `routes/line.tsx` · `board/BoardConsequence.tsx` |
| ⚪ | **`ReviewFrame` claims fields that a replayed frame does not carry.** The union types every variant with a `seq` and a `line_id`, and `replay.frames_from` writes neither on the notice-level reasoning lines — so the client's types describe a shape the server does not produce, and the reducer reaches for `frame.line_id` behind an `'line_id' in frame` guard that exists only because the compiler is right and the type is wrong. Nothing is broken today because every consumer reads the fields it needs, but it is the kind of lie that survives until somebody trusts it. The honest shape is a base with optional line-scoping, or a separate `ReplayedFrame` type. | `lib/api.ts` · `api/replay.py` |
| ⚪ | **Two docstrings in `engine/rules.py` still name the `NOT_ASSESSED` constant that P2 deleted.** `rules.py:46` and `:1284` explain a boundary by pointing at a symbol that no longer exists, so a reader follows the reference and finds nothing. `roles.py:112` names the label in prose for the same reason. Three sentences, no behaviour. | `engine/rules.py` · `roles.py` |
| ⚪ | **The substitution runs four real steps and narrates none of them.** `kicad/board.py:158-215` places the part, carries its nets by function, refills the zones on both copies in separate interpreters and runs DRC twice, and the screen shows two pictures at the end. The *watch it work* beat is available for free from events that genuinely happen. Related: the swap is deterministic `pcbnew`, not a model, and the product never says so on screen, which gives away the strongest half of the claim. [R9](RESEARCH-3rd-Passthrough.md#r9-whether-a-model-should-make-the-board-change-and-gpt-6-astra) | `board/BoardConsequence.tsx` · `api/boards.py` |
| ⚪ | **A pinout published as a figure blocks a board consequence, and an image-capable model would unblock it.** `LD1117S33TR` has no entry in `kicad/catalogue.py` because ST publishes its pin connections as a drawing, so the product refuses that part by name. `gpt-6-astra` takes image input and scores 95.9% on BenchCAD, and our `llm.py` already speaks the OpenAI-compatible API, so the provider is a base-URL change. The engine would still verify the reading: the swap maps by function and DRC runs before and after, so a wrong pinout shows as broken connections rather than as a confident answer. Cost is the question, not capability, at $10/$50 per million against DeepSeek. [R9](RESEARCH-3rd-Passthrough.md#r9-whether-a-model-should-make-the-board-change-and-gpt-6-astra) | `kicad/catalogue.py` · `llm.py` |
| 🟡 | **`/changes` is three screens stacked in one reading column**: the notices list, the live concurrent review, and one change request per affected line, in that order down `routes/changes.tsx:160-280` inside `width="reading"`. The run is hard to read while it happens and impossible to compare afterwards, and there is nowhere for the board, the bill change or the question to live per lane. A master-detail split, one open lane at a time with the others still advancing, is the shape the published patterns converge on. **decision**, and it is a redesign of the busiest screen in the demo. [R3](RESEARCH-3rd-Passthrough.md#r3-the-changes-page-is-doing-three-jobs) | `routes/changes.tsx` |
| ⚪ | **The changed board is written and then deleted.** `kicad/board.py:191` writes a real `kicad-after-{refdes}.kicad_pcb`, and `api/boards.py:312` removes the whole scratch directory. Nothing can be exported, so *open it yourself* is not an answer this product can give. Returning that file as a download is one endpoint, with a precedent at `api/app.py:704-708`; the project zip and `kicad-cli` fabrication output are the two levels above it. [R4](RESEARCH-3rd-Passthrough.md#r4-the-board-in-the-change-request-and-exporting-it) | `api/boards.py` · `kicad/board.py` |
| ⚪ | **The first board render of a process still costs about three seconds.** It is cached by the bundle's digest afterwards — measured 3365 ms cold, 25 ms warm — and the product line page warms it on arrival, so the BOARD toggle is a switch rather than a wait. What remains is that the cache is in process memory and empty after a restart. | `api/boards.py` |
| ⚪ | **A seeded design run has no LangGraph checkpoint, so it cannot be resumed.** It restores, renders and reads correctly — `_board_from_frames` rebuilds the board from the frames — and `resumable` is false, which is accurate. Continuing one would need a checkpoint the seed does not write. | `tools/seed_world.py` |
| ⚪ | **The power tree scrolls rather than shrinking below about 700 px of pane.** It is one size everywhere now — `buildGraphLayout` sizes the picture from its content and the pane scrolls when it cannot hold it — which is right for a diagram whose labels are part numbers and means a laptop shows two thirds of it at a time. A fit-to-pane control, or dropping the tier columns to the two that a three-part board uses, would close the rest. | `design/ComponentGraph.tsx` |

| 🟡 | **The seed is the only writer of verified part facts.** `tools/seed_world.py` marks the PARTS.md readings as verified, so the datasheet-outranks-listing rule now fires — but only inside the demo company. A real account sourcing a part live still gets the distributor's parametric table for every engineering figure, because nothing verifies a reading at runtime. The θJA extractor could mark its own findings; it does not yet. | `parts/normalize.py` |
| 🟡 | **The AML gate makes every unseeded organisation's boards fail once a list exists.** Turning on an AML reports every part not on it, which is correct — and means a company must qualify its whole existing BOM before the gate is usable. The seed does this by deriving the list from what ships; a real company has no such step and no screen to do it on. | `api/lines.py` |
| ⚪ | Two accounts share one password in the seed, and it is in the repository. Fine for a demo world in a scratch database, and it must never be run against anything else — the script takes an explicit database URL and has no default, which is the only guard. | `tools/seed_world.py` |
| 🟡 | **The AML and AVL can only be edited through the store.** `keep_lists`, `qualify_part` and `approve_vendor` have no endpoint and no screen, so a company cannot state its own policy without a Python shell. The gates read the lists correctly; nothing writes them but tests and the seed. | `api/lines.py` · item 16 |
| ⚪ | The matrix's `ambiguous` banner — one MPN listed by two manufacturers — is not browser-verified. The endpoint path is tested both as a unit and through HTTP; only the rendering is unseen. | `routes/matrix.tsx` |
| ⚪ | **Item 6, `power_dissipation_max`, is not built.** Nothing states the quantity it checks: every distributor "power" field is a different measurement, and AMS1117 and NCP1117 both say *"Internally Limited"* while TLV1117LV says *"See Thermal Information"*. For a linear regulator the dissipation limit is the thermal one. The rule's real subject is passives — a 0603 resistor's 100 mW rating is a genuine spec — so revisit when a board carries resistors. | `engine/rules.py` |
| ⚪ | **`footprint_compatibility` compares land patterns, not pin functions.** A candidate with the same footprint and a different pinout passes. Pin names reach the system only for bus masters and only to count GPIOs, so `PartSpec` keeps the count and not the names — comparing pinouts means a distributor call for every part on every board. The land pattern answers the decisive question; the pin map is unbuilt rather than half-done. | `engine/rules.py` · `parts/normalize.py` |
| ⚪ | **`p_dis_min` and `approved_only` are absent from `CONSTRAINT_FIELDS`,** deliberately. Neither has a consumer — item 6 was not built and the AML/AVL layer arrives at item 13 — and a key the model may emit that nothing reads is accepted, ignored, and looks like it worked. That is how `vout` sat in the vocabulary being silently dropped. Add each one *with* its consumer. | `reviewer.py` |
| ⚪ | The θJA package table is knowingly optimistic for SOT-223 — 62 °C/W against a 66–136 spread TI measured. Left alone by decision: it is a fallback so a part with no published figure does not halt a run, no cell in the demo depends on it, and no single number is defensible when the board's copper is unstated. | `engine/packages.py` |
| ⚪ | Most rows of that table have never been checked against a datasheet. Seven have; the module docstring names which. | `engine/packages.py` |
| ⚪ | `QFN-32-EP(5x5)` and its siblings resolve to no θJA at all. The exposed-pad guard refuses to match `QFN32`, whose values already assume a thermal pad, so QFN regulators go thermally unchecked. | `engine/packages.py` |
| ⚪ | The conflict drawer's `FAILED · ACCEPTED` label is not browser-verified. The drawer only opens on an *open* conflict, and a waived one is by definition closed, so no restored run could exercise it. Typechecked and reasoned through; the sibling path in `ChatPanel` was verified live. | `ConflictPanel.tsx` |
| ⚪ | Codex cannot run the full test suite — its sandbox is denied the PostgreSQL socket. Every delegated task has reported a partial count, and twice the full run found failures the partial did not. Every agent result needs the suite run again by hand. | tooling |
| ⚪ | **A profile's rail statements are still not applied to a *design* run**, and that stays deliberate: a design run's rail ids come from the planner, so a stored `3v3` meeting a planned `3V3` would raise mid-run and kill the board rather than correct it. Item 12 resolved the case that mattered — `profile.board_from` builds a board from the line's own BOM, where the rail ids and the profile come from one place. | `graph/nodes.py` |
| 🟡 | **A deployed instance has no KiCad, so the board section is unavailable there.** The capability is configured with `CONTINUITY_KICAD`, and locally that is the pinned container; Render would need either an image carrying KiCad or a service beside the app to run it. Every route degrades honestly — the project is still stored, and the screen says the instance has no KiCad rather than guessing — but the demonstration's board views only exist where one is configured. | `kicad/runner.py` · deployment |
| 🟡 | **LD1117S33TR has no pin functions on file, so it gets no board consequence.** ST publishes the pin connections as a figure and a figure is not extractable text, so the part is deliberately absent from `kicad/catalogue.py` and the screen says why. Reading a pinout out of a drawing is a different capability from reading a table. | `kicad/catalogue.py` |
| ⚪ | **A retired part at more than one position on a board is refused rather than substituted at each.** The consequence names the reference designators it found and stops, because substituting several positions at once means several swaps in one file and a report that has to say which finding belongs to which. | `api/boards.py` |
| ⚪ | **The cheap-part-costs-a-respin beat is researched and not in the demo.** `ME6211C33M5G-N` is in the pinout catalogue and the KiCad tests use it, but it is not a sourced candidate: its datasheet gives 6.5 V absolute maximum input and 600 mA, which rules it out on the 12 V cabinet controller and leaves it viable and cheaper on the sensor node — where the board says it costs a layout revision. Adding it as a real part needs PARTS.md readings and a live listing, which is a flow decision rather than architecture. | `tools/eol_differential.py` · PARTS.md |
| 🔵 | **Outbound mail is still deferred; inbound is being built as Phase 5.** Inbound reads a mailbox over IMAP, which needs an account and an app password and no public URL. Outbound still needs SMTP or a transactional API key with a verified sender domain, and stays deferred. Neither is hard and the code is transport-agnostic — a poller is a loop around `notices.read`, and the inbound webhook of any provider posts the same body `POST /notices` already accepts, so inbound is close to zero new code once an account exists. **The demo does not depend on it:** the POST path is verified live, with a real PDF, through the live model. What is genuinely missing is the *outbound* approval request — a gate currently records the decision and does not ask anybody for it. | `api/notices.py` · item 14 |
| 🔵 | ~~The operating profile is not persisted.~~ Ambient, copper and rail load reach the engine but live only in a fixture; storing them against a product line is item 10, which is where product lines first exist. **Resolved by item 10b, 7 Sep** — see the rail caveat above. | `api/store.py` |

## The first flow pass, 8 Sep

Sparsh ran the app end to end for the first time since the reframe. Every finding was one
symptom of a single mistake: **2.0 was built as endpoints and three new screens hung beside
the 1.0 shell, and the old centre of gravity was left in place — a product line was still "a
design run that has not happened yet".**

**All eight 🔴 rows from that pass are resolved**, by items 19 to 30, and moved below with
what fixed each. What is left here is what that pass turned up alongside them and nobody has
worked yet. Audited against the code on 10 Sep.

| | Item | Where |
|---|---|---|
| ⚪ | **Expired sessions are never deleted.** `store.delete_expired_sessions` has no caller. Not a hole — `user_for_token` enforces `expires_at > now()` in the same statement it renews with, so an expired session cannot authenticate — but the rows accumulate for ever and nothing prunes them. Found by the same sweep, 9 Sep. | `api/store.py:392` |
| ⚪ | **A part that fails its check names the failure in the review pane, not on the part.** The pane now reads the `detail` every failing slot carries — *"U1 fails a check on this product line"* with the rule's own sentence under it — so a red part is no longer silent, which is what made this 🟡. It is still one pane away from the node it is about, and the sentence on the part itself, on hover, is the better answer. Nothing fails on the seeded world, so this shows only when somebody asks what happens when one of them is wrong. | `routes/line.tsx` · `review/ReviewTrace.tsx` |
| ⚪ | **The check sends three fields the page never reads.** `not_assessed`, `evidence_missing` and `graph.off_tree` are computed, serialised and dropped — `checked` is read now, in the header chip, left over from the coverage paragraph that was removed. They belong in the change request rather than on this page, so either move them there or stop computing them. Payload nobody reads is how a field quietly stops being correct. 9 Sep | `api/lines.py` · `linegraph.py` |
| ⚪ | **`_resolve_quietly` now swallows every exception.** Widened 9 Sep so a distributor timeout cannot take down a page whose job is to say whether a product is sound, and the caller falls back to the company's own readings. It is logged at warning, but a bare `except Exception` in the resolution path hides a real defect in normalisation as easily as a network failure. Worth narrowing to the transport and parsing errors that actually occur, once there is evidence of which those are. | `api/review.py:100` |
| ⚪ | **`POST /notices/{id}/review` is now unreachable from the app.** The run writes the change request itself, so the one-shot endpoint has no caller — its `GET` sibling is still what lists stored requests. Keep it or delete it, but a route nothing calls and nothing tests through the UI is a route that will rot. | `api/notices.py` |
| ⚪ | **Two SSE readers.** `lib/sseClient.ts` drives the design session as a singleton — one listener, one abort controller — so the review could not share it without taking its place, and `lib/reviewStream.ts` has its own copy of the read-frames loop. One of them should own the parsing. Not done mid-phase because the design client is what the whole existing demo runs on. | `lib/sseClient.ts` · `lib/reviewStream.ts` |
| ⚪ | **The walkthrough's replay machinery is still inside `sseClient`.** `releaseWalkthroughReplay`, the milestone type and the pacing gate have no caller now that the tour is gone. Unreachable rather than harmful, and it should go with the SSE refactor above. | `lib/sseClient.ts` |
| 🟡 | **A part fitted at two positions on one board is substituted at one of them.** `_slot_of` returns the first slot carrying the retired part, so a product with two of the same regulator gets a proposal about one and silence about the other. Same class as the KiCad limitation above, and the two should be fixed together. | `api/review.py` · `kicad/board.py` |
| ⚪ | **Applying a substitution moves the revision and leaves the profile citing the old one.** The Gateway is Rev D and its ambient still reads *"gateway operating profile Rev C — 45 °C sealed-enclosure ambient"*, because that is the sentence somebody wrote when they described the board. Honest, and it reads as a mismatch. The profile's sources should either travel with the revision or say which revision they were written for. | `api/review.py` · `profile.py` |
| ⚪ | **`manufacturer_of` takes the first manufacturer any bill records for a part.** Two product lines fitting the same part number from different manufacturers resolve the incumbent with whichever row the database returns first. Correct for every board that agrees, arbitrary for one that does not. | `api/store.py` |
| ⚪ | **A product line's notice banner links to the notices screen rather than to that notice.** One click too many, and the wrong one when several are open. | `routes/line.tsx` |
| 🟡 | **The quality gate only fires when no approved part clears the board, and in the seeded world one does.** TLV1117 is on the AML because the Bench supply ships it, and it clears the Gateway with room to spare, so the run proposes it and the decision stays engineering's — correct, and cheaper, and it costs the demo its strongest thirty seconds. Under live sourcing the gate does fire, because TLV1117 is excluded as ambiguous, which is a fragile reason for a beat to exist. The flow decision is whose to make: a fourth product that ships something else, a candidate set that leaves TLV1117 out, or accept that the honest answer here is the cheap one. **Still open after Phase 8**, and narrower: the Gateway now stops at *procurement* on stock, so one notice does produce two different outcomes, but the quality gate itself still does not fire because no line's best answer is off the approved list. **Superseded 10 Sep** by the 🔴 above: the cause is not the seeded world, it is `review.choose` hardcoding engineering for any clear candidate, and the scenario names a department this product does not have. See [R8](RESEARCH-3rd-Passthrough.md#r8-the-cross-team-response-is-the-problem-statement-and-one-desk-answers-everything). | `tools/seed_world.py` · flow |
| ⚪ | **The board's power tree is drawn from the profile, so a line with no profile shows unconnected parts.** Correct, and thin: the connectivity is in the schematic of an attached KiCad project and nothing reads it. Deriving edges from the board's nets would give a real netlist for any line with a board attached. | `linegraph.py` |

## Resolved here, kept for the reasoning

| | Item | Where |
|---|---|---|
| ✅ | ~~**A past design run is now reachable from nowhere.**~~ — 11 Sep. The design route is keyed by the line and opened `threads[0]`, so the first run was orphaned the moment a second one existed and nothing could name it. The product line page now carries a **RUNS** button beside the end-of-life one — disabled while the line has never had a run, because a control that opens an empty drawer is an affordance with nothing to do — and it opens a drawer listing each run with its prompt, its status and how much of the board it placed. Each entry links to `/design/{line}?thread={run}`, which is the address a run did not have: the route reads the query and opens the run it names, falling back to the newest for an id that is not this line's. **Verified in a browser against a line given a second, older run**: `Runs (2)`, two entries with their own links, and following the older one fetched `/threads/older-run-check/board` and put its restored workspace on screen. | `routes/line.tsx` · `routes/design.tsx` |
| ✅ | ~~**The landing page threw during its own animation, and had since the legend stopped being a fixed three rows.**~~ — 11 Sep, found by signing in to check something else. `legendRows(slots.map((slot) => slot.status))` looks each status up in `LEGEND_ROWS`, and the landing page replays a **stored** walkthrough: a recording carries the vocabulary of the day it was made, and this one still contains `not_assessed`, deleted from the engine when the three rules it covered became real checks. The lookup returned `undefined`, the destructure in the render threw, and the first screen a visitor sees became an error boundary instead of the application — on a page that nothing had opened since the change. Deriving the legend is right; what it was missing is that **a recording is old by nature**, so a state this version cannot name is now dropped rather than drawn. The test pins the legend's **length**, because `toEqual` treats an `undefined` element as no element at all and the first version of it passed while the crash was still there. Nothing else in the frontend indexes a status into a table, which was checked rather than assumed. | `design/ComponentGraph.tsx` |
| ✅ | ~~**A replayed review cannot say where each candidate came from.**~~ — 11 Sep, and the row was right that it is one field. `Attempt` carries an `origin`, `review.attempt` passes it through, and the run writes it into `decisions.document` beside the manufacturer and the package, so a replayed lane repeats the sentence the live trace said. **Why it was worth doing is written in the module it fixed**: `replay.py` opened with a paragraph explaining that an origin could not be reconstructed and would have to be guessed, which was true, and was the product admitting a hole in its own default mode — replay is what a judge sees unless somebody presses RUN IT AGAIN. A decision stored before today carries no origin and replays with the bare part number rather than an invented one; three tests pin that, one for the sentence, one for the old record and one for a blank field that must not read as a provenance. **Verified on a rebuilt world end to end**: five candidates across three lanes, and the replayed lanes read exactly what the live trace read. | `review.py` · `api/review.py` · `api/replay.py` |
| ✅ | ~~**The θJA extractor declines on row-per-package thermal tables.**~~ — 11 Sep. onsemi's NCP1117 prints every label first, then every symbol, then every value, then every unit, so its θJA row carries no number and the column binding had nothing to hold on to. **The binding that was available is the one a person would use**: the figures run in the order the labels do, so the i-th labelled row carries the i-th value, and a row belongs to the last package heading printed above it — found with the candidate keys `_packages_overlap` already reaches for, because `_names_a_package` splits `SOT−223` at its U+2212 and resolves neither half. **Which row answers the question is decided by the verifier and not by the model**: the reply names a figure and a row, and the document is asked which package governs that row and what that position holds — so the right row with the wrong number in it is refused, and so is the right number out of the other package's row. Verified against the real document rather than a reconstruction of it: the model's own reply returns 160 °C/W as `Case 318H (SOT−223)`, 67 for the SOT-223 part is refused as belonging to the DPAK row, and the live end-to-end path returns the same fact. One of the nine tests is the linearisation itself, copied whole and including the mangled `R/C0113JA`. | `parts/datasheet.py` · `tests/test_datasheet.py` |
| ✅ | ~~**A mailbox poll is a full IMAP login every fifteen seconds, against an account shared with nothing but a demonstration.**~~ — 11 Sep, and the row arrived as an incident rather than as a finding: **Google closed the account twice in one day**, the first time after an orphaned server from 9 September had been polling for thirty-nine hours, about nine thousand logins. It surfaces as `[ALERT] Invalid credentials (Failure)`, the same alert a revoked password gives, so the first hour of it went into regenerating a password that had never stopped working. SMTP says the useful thing — `534 5.7.9 Please log in with your web browser` — and that is what identified it as a throttle. **The documentation had the answer the whole time**: Google's client guidance is *check for new messages every 10 minutes*, and this was forty times that. So the poller has two rates now, fifteen seconds **while a browser is asking for notices** — the arrivals poll every authenticated screen already runs, which is why no particular page has to be open — and Google's ten minutes otherwise, slept in slices so a browser that opens part-way through the nap is served in seconds. The failure backoff stays, and its ceiling moved above the idle interval because a ceiling below it would make a *failing* mailbox faster than a healthy idle one. **IDLE was considered and not taken**: Gmail supports it, and Google's extensions page says to re-issue the command every few minutes under load, which is a keepalive timer and a reconnect path this module deliberately does not own. | `continuity/mail.py` · `api/notices.py` |
| ✅ | ~~**A processed message stays in the inbox looking unprocessed.**~~ — 11 Sep. `mail.move_to_processed` files a message into `Continuity/Processed` once it has become a notice, and **leaves the ones that held none where they are**, which is what makes the inbox itself the report: what is left in it is what was not a change notice. Two of the row's own decisions kept: a move rather than a `\Seen` flag, because the read position is a stored UID precisely so that flag stays the person's to set; and `imap_tools` sends a server-side `UID MOVE` where the server advertises one, falling back to `COPY` plus `STORE \Deleted` where it does not. **One connection of its own, and only when there is something to move**, so a poll that stored nothing does not authenticate twice — a poll is a full login, and the logins are what closed this account twice in one day. Five tests, and the wiring was confirmed by unwiring it: the failing-move test asserts the move was *attempted*, because without that it passed while nothing was called at all. | `continuity/mail.py` |
| ✅ | ~~**`./demo.sh --check` printed `mailbox configured` while the mailbox was refusing every sign-in, and its footer said the read position had been moved whether or not it had.**~~ — 11 Sep, found by living through it. The three variables were present all through the hour Google refused this account, so the preflight passed and step 4 was the first thing that could find out otherwise. The check now **signs in** (`mail.reachable`) and prints the server's own words when it will not; the footer asks the database for the stored position instead of asserting one. **The state that made the footer wrong had teeth**, and this is the part worth remembering: a world whose seed cannot reach the mailbox keeps **no read position**, and `search_from(None)` returns `ALL` — so the poller read the entire inbox that afternoon, which is how an older notice email sitting in it came to be read and refused. `poll_once` now catches up before it reads anything when no position is stored, which is the same statement the seed makes when it *can* reach the mailbox, and it raises rather than reading from the top when it cannot. **`--no-mail` also does what it says now**: the app calls `env.load()` itself, so leaving the variables out of its environment did nothing and the poller ran anyway, on the day the polling is what closes the account. | `demo.sh` · `continuity/mail.py` |
| ✅ | ~~**Most recorded readings have no source line, so the evidence says *source unavailable* where PARTS.md has a quote.**~~ — 11 Sep, and the row undercounted it: **fifty-seven** of the demo world's seventy-eight readings had no line, not fifty-five, and the count is now **four**. Each was filled from the line that actually states it — PARTS.md's own quotations where it has them, the manufacturer's document where it does not, which meant eight datasheets. Three of the eight are worth recording. onsemi's and ST's both refuse a scripted download, so those two came through a PDF parser rather than curl. And ST's current document for the MCU is **DS5319 Rev 20**, which st.com will not serve to a script and whose only reachable mirror is a **July 2007 Rev 2 marked *Preliminary*** — so the STM32F103C8T6's four readings stay unsourced **on purpose**, which PARTS.md's own LD1117 lesson says is the better of the two wrong answers. A test pins the exact set, so a field added tomorrow without a citation fails the suite rather than the demo. **Verified on a rebuilt world through the real `/matrix` endpoint: 121 evidence rows on three cells carry a real line, and every blank left is the board's mounting condition**, which is its own row above. | `tools/eol_differential.py` · `tests/test_eol_differential.py` |
| ✅ | ~~**A recorded reading's datasheet sentence is stored and never shown.** `cite()` renders a dossier-sourced fact as `<field> (dossier)` with the source *"Continuity dossier — learned in an earlier run"*, so the line the reading was actually taken from — which `part_facts.source` holds, and which PARTS.md promises for every figure — never reaches the drawer.~~ — 11 Sep, and **narrower than the row said**: the `normalize` path has always carried the source as `Continuity dossier — learned in an earlier run (the line)`, and only `part_from_facts` — the fallback a product line's own parts are built by, which is where most evidence comes from — threw each one away and stamped the bare label. One line, and the drawer now reads **`vmax (dossier) = 15.0 <- Continuity dossier (datasheet — …)`**. Verified on the live seeded world through `/matrix`, which is the same evidence the drawer renders. **What that then exposed is its own row below**: most readings have no source line recorded at all. | `parts/dossier.py` |
| ✅ | ~~**The parse cache is keyed by MPN alone, so two companies' listings of one number share one parse.** `normalize._cache_path(mpn)` is the whole key, and the cached fields are the engineering ones — supply ceiling, current, temperature range. JLCPCB lists `TLV1117LV33DCYR` under Texas Instruments at 5.5 V and under JSMSEMI at 12 V, and whichever normalises first decides what the other one is.~~ — 11 Sep, the fourth instance of this and the first one that was a cache. **The key is the listing, not the part number**: the manufacturer is part of the filename, because the filename is the lookup, and the same argument `_prompt_version` already makes applies — an answer to a different question is not a cached answer. Proved with two listings of one number that disagree: before, JSMSEMI's part came back carrying TI's 5.5 V ceiling; after, each keeps its own and the first is still itself when it comes back off disk. **Measured, because the row warned the re-key would cost a recording pass: a replayed review is 1.74 s cold and 0.33 s warm**, so the one-time cost is about a second and a half on the first review after the change, and eleven listings re-parse. The cache does not travel, so every clone already paid that once. | `parts/normalize.py` |
| ✅ | ~~**A notice can only be reviewed when the retired part sits at the same refdes on every affected line.** The slot is looked up per line from its own BOM, but `evaluate_matrix` takes one slot for the whole grid, so U1 on one board and U7 on another is a 409 telling the caller to name one.~~ — 11 Sep. The slot was already looked up per line; what refused was the grid, which could only name one. **`evaluate_matrix` now takes one position or one per line**, `Cell` carries the position it was substituted at — because boards disagree and the headline a desk reads names that position — and `Matrix.slot` is `None` when the grid spans several, since naming one of them would be a lie. The precedent signature follows the cell rather than a parameter, which removed a second copy of the same answer. Verified on the real seeded world: the demo's boards carry the regulator at **U3, U1 and U2**, and one call now reviews all three where it used to say *name one with `slot`*. A caller who does name a position still gets it applied to every board, because then they have said which they mean. | `matrix.py` · `api/notices.py` |
| ✅ | ~~**The outbound approval request does not exist.** A gate records the decision somebody made and never asks anybody to make it.~~ — 11 Sep, and **the row's own proposed fix was already built**: the in-app inbox it names as the demo-safe half is `GET /decisions` and `/approvals`, which item 36 landed. Verified on the live world — signing in as procurement names three decisions across three product lines, each with `mine` saying what that desk still owes, and the rail carries a count. A desk is asked, and it is asked without being told which product line to open, which was the whole of the row's complaint. What remains is **delivery outside the app**, and that is the outbound-mail 🔵 below rather than a separate item. | `api/decisions.py` · `routes/approvals.tsx` |
| ✅ | ~~**A pending decision replays without its question.** A review that ended waiting on a desk shows its trace and its proposal when the line is reopened, but the APPROVE AND APPLY block does not come back — the frames carry `line_done`, deliberately not `question`, because whether that decision is still answerable is the decision row's business rather than the trace's.~~ — P6, 11 Sep, and it **was** the notice-level endpoint's business: `GET /notices/{id}/reviews` rebuilds the question with the same `decision_text` the run used and places it before the ending, where the run asked it, and carries who has signed so far. The company view re-raises it with live buttons — a replayed decision is answerable, not just readable. | `api/notices.py` · `api/replay.py` · `review/laneState.ts` |
| ✅ | ~~**No change request shows a board**, and **the consequence is computed on demand and never stored** — so the strongest artefact in the product sat behind a button nobody presses, and the person who reads the request tomorrow sees the proposal and the cost without the layout consequence.~~ — P8, 11 Sep, both rows together, since the second was the cause of the first. The review **fires each line's KiCad consequence when its proposal is chosen** and carries on: the request row is written milliseconds later and the placement lands on it seconds after that, while the question is already waiting on desks. The card renders the pictures from the stored payload with **no button**, because a control offering to compute what the document already contains is an affordance with nothing to do; a request without one — a world with no KiCad — keeps the button it had. **Measured: the replayed run is 0.247 s**, unchanged, because the work is off the stream, and all three boards land within about fifteen seconds. The task is held in a module-level set rather than scheduled bare: the event loop keeps only a weak reference and a placement several seconds long can be collected mid-flight, which the same guard in `normalize` and `app` already exists for. | `api/boards.py` · `api/review.py` · `api/store.py` · `review/RequestCard.tsx` |
| ✅ | ~~**Three of the review's own candidates come back *not found* on the prefilled matrix.** `LD1117-3.3`, `SPX1117M3-L-3-3/TR` and `XBL1117-3.3` are the names JLCPCB's package search returns, and `matrix_api.resolve` looks a candidate up by exact MPN — so the review evaluates them and the matrix reports *not found at the distributor, so not checked*.~~ — P7, 11 Sep. **The spike settled it: an exact-MPN search does return all three**, so the fixtures close the replay gap and the repair is plumbing. The review now says which listing it weighed: `md|manufacturer` pairs travel in the link, `MatrixRequest.candidate_manufacturers` carries them, and `_build` prefers what the caller resolved over the company's record. **Two things had to be right for that to work at all.** `change.Alternative` gained `manufacturer`, and the change request's serialiser writes that list by hand — a field added to the dataclass and not to `to_json` is silently absent, which the first attempt did and the seeded end-to-end test now catches. And the **list separator is a newline, not a comma**: `UMW(Youtai Semiconductor Co., Ltd.)` is one of the demo's own candidates, so a comma-joined list split in the middle of a company and the truncated name matched no listing. That was found by the test written for the separator, not by using the product. Verified live: the prefilled grid carries **all six candidates as columns and eighteen checked cells**, with no *not found* line, and each failure naming its desk — `voltage overlap · DESIGN` on the cabinet, `availability · PROCUREMENT` on the Gateway. | `change.py` · `api/matrix.py` · `routes/matrixLink.ts` |
| ✅ | ~~**A running review is lost on navigation and cannot be replayed.** All lane state is `useState` inside `ReviewLanes.tsx:95-100`; leaving the page unmounts it and aborts the stream, and returning runs `open()` which fetches the stored change requests, so the finished documents appear where the run was. `api/replay.frames_from` already rebuilds a line's trace from `decisions.document` and `GET /lines/{id}/reviews` serves it for one product.~~ — P6, 11 Sep. `GET /notices/{id}/reviews` is the same assembly keyed by notice, which is the page a company-wide run is started from, and `api/notices.py` asks the question again through `decision_text` so a **pending** decision replays able to be answered rather than as a verdict with no way to sign. **The frame-building moved out of the component into `review/laneState.ts` and both paths go through one reducer**, which is what makes a hydrated lane and a live one the same object rather than two definitions waiting to drift. Two things the first pass got wrong, both found by looking at the screen: `frames_from` did not say which line a frame belonged to, so every narration line landed in the preamble and the lanes came up empty — it now marks the frames that are a board's and leaves the two opening notice-level lines unmarked; and it asked the question *after* `line_done` where the run asks it before, which is a trace nobody ever saw. Verified in the browser: a fresh page load on a reviewed notice shows three lanes, their full traces, and all three questions with live buttons, with nothing pressed; `RUN IT AGAIN` still streams the live path through the same reducer; and the browser spec now reloads and asserts the review comes back. | `api/notices.py` · `api/replay.py` · `review/laneState.ts` |
| ✅ | ~~**`signal_integrity` is `evidence_missing` on all three change requests**, which is the admission P2 existed to remove, on the document a judge reads. The rule is built and its arithmetic is right; what is missing is its inputs — `vout_accuracy_pct` and `load_regulation_pct` are not on the parts, so the rail's worst published deviation cannot be stacked and the rule correctly declines.~~ — 11 Sep. **The figures were researched with P2 and then dropped on the way to the engine**: they lived on the fixture parts the unit tests use and neither was on `DOSSIER_FIELDS`, so they could not survive the company's own record. The rule was green in the tests and declining in the product, which is the shape of failure this file keeps finding. Both fields are storable, typed and engineering-grade now, and the four regulators carry sourced readings: TI's ±1.5% and 35 mV are quoted, and AMS1117's ±3.0%, LD1117's ±2.0% and NCP1117's ±2.0% are **derived from each datasheet's published output window** and say so where they are recorded. Two of the four were placeholders before — AMS1117's was `1.0/1.0` with no source at all, and LD1117's used the front page's ±1% trim, a 25 °C figure, where Table 6 guarantees ±2% over temperature and the board runs at 55 °C. **Verified on the live seeded world: `no_evidence` is empty on all three requests**, the resting check reports no missing evidence, every cell of the three-by-four grid is satisfied, and the demo's story is untouched — NCP1117 still rejected at 159 °C on the Gateway, TLV1117 still failing the cabinet on 5.5 V. | `parts/dossier.py` · `tools/eol_differential.py` · PARTS.md |
| ✅ | ~~**Two notices about the same part are indistinguishable in the list.** Every entry is labelled with its MPN alone, so the full notice and the preliminary one both read `AMS1117-3.3` and the only way to tell them apart is to open each. The received date is stored and the reference number is in the document; neither reaches the list.~~ — P9, 11 Sep. The reader now takes `reference` and `reference_line` under the same discipline as every other field it reports — the line must be in the document **and the value must be on that line**, which the first pass of this item was missing and which the reference shares with `replacement_mpn` for the same reason: quoting *a* line is not sourcing *this* value, and a reference hung off a line that does not carry it is a citation that sources nothing. It is stored on `notices`, returned by `GET /notices`, and the list row now reads **`AMS-PCN-2026-114 · received 11/09/2026`**, falling back to the date alone for a notice that carries no number, which preliminary notices often do. **The date is rendered in the reader's own timezone**, not `created_at.slice(0, 10)`: a UTC truncation shows yesterday's date to anybody east of Greenwich before 08:00, and a date that can be a day wrong is worse than no date on the one row whose whole job is telling two same-part notices apart. Seen on screen — the slice read `2026-09-10` where the local day was `11/09/2026`. Verified live: both committed notices delivered, two rows, two distinct references, the preliminary one showing no last-order date and no recommendation. | `notices.py` · `api/store.py` · `routes/changes.tsx` |
| ✅ | ~~**Three credentials were printed into a session transcript on 11 Sep and have not been rotated.**~~ — 11 Sep, by Sparsh, the same evening. A broad `grep '^DATABASE_URL\|^CONTINUITY'` over `backend/.env` while scoping P1 swept up everything the file holds, which is the anti-pattern `common/security.md` names by name: the model key and the mailbox app password both went into the transcript and **both were rotated**. **No database credential was exposed** — by the time of the sweep `.env` held `postgresql:///continuity_demo`, a local socket URL with no password in it, so the row's original claim that the Neon password had leaked was wrong about its own incident. Naming the local database in `.env` was the hazard removal and the rotation was the remediation, and they are different acts; the row stays here with both recorded so the next person does not conclude that editing a file is enough. | `backend/.env` |
| ✅ | ~~**The graph's legend reads *Valid / Conflict / Pending* on a board at rest.** A product line's nodes are all `unchecked` — fitted, and looked at by no rule in this session — and the legend offers three states none of them are in. Correct during a run, wrong on the product line page, and the fix is a legend that knows which of the two it is looking at.~~ — P10, 11 Sep. The legend is derived from the states actually present on the board it was handed: a resting board offers *Fitted · unchecked*, a running one the states it has. A board at rest no longer names a conflict nobody has raised. `legendRows` is a pure function with two tests, one per case. | `design/ComponentGraph.tsx` · `design/ComponentGraph.test.ts` |
| ✅ | ~~**The three not-assessed rules are explained at paragraph length on the validation trace.** `engine/rules.py` returned emc, output capacitor stability and signal integrity as `not_assessed` on every board, and the trace printed all three with their prose.~~ — P2, 11 Sep, and the decision went further than the row asked. The morning plan kept the admissions and moved them off the trace; Sparsh overruled it — *either the three check something real or every mention goes*. They are real checks now: the published stability condition per regulator, the candidate's regulation type against the part it replaces, and the rail's worst published deviation against every load's supply window. **`not_assessed` is deleted from the vocabulary**, so the label, the `RULES` entry, `CheckStatus`, `change.py`'s field, `api/lines.py`, the matrix's counts and `matrix.tsx`'s label all went with it. All three are satisfied on the demo world, so none takes over the story the other fourteen tell — verified live: the Gateway still stops at TLV1117 with NCP1117 rejected at 159 °C. Two docstrings in `engine/rules.py` still name the deleted `NOT_ASSESSED` constant; that is its own row. | `engine/rules.py` · `roles.py` · `matrix.py` · `lib/api.ts` |
| ✅ | ~~**`Could not be checked: voltage overlap` is a missing fact, not a boundary**, and reads identically to the three above it. It is `evidence_missing` because CL31A226KAHNNNE and TLV1117LV33DCYR publish a maximum and no minimum.~~ — P2d, 11 Sep. Five parts gained a `vmin`: TI's 2.0 V is quoted from the recommended operating conditions, the Samsung capacitor records −25 V as a non-polarized part whose rating is symmetric, and the three LDOs that publish no minimum record *Vout + dropout* with the derivation named in the source line rather than dressed up as a quotation. Verified on a rebuilt world: every *publishes no minimum* line is gone from every matrix cell, and `voltage_overlap` is `evidence_missing` nowhere. The row's sibling expectation — `no_evidence` empty on all three requests — did not hold: see the `signal_integrity` row. | `tools/eol_differential.py` · PARTS.md |
| ✅ | ~~**A live review is silent for as long as the catalogue takes, and the catalogue is unbounded.** `_catalogue_search` walks up to `CATALOGUE_POOL = 25` hits calling `sourcing.choose` on each **sequentially**, and every one is a distributor lookup and a datasheet read. Measured against a live JLCPCB: 52 seconds one day, still unfinished after 140 the next.~~ — P11, 11 Sep. Hits are filtered cheaply first — the retiring part's own input voltage, the same-part guard — then normalised **a batch at a time and concurrently**, in pool order, stopping as soon as `CATALOGUE_LIMIT` is met. Order is preserved, so the acceptance test is that the demo's candidate set is byte-identical: verified on a rebuilt world, the same five candidates in the same order, the same three answers, the same verdicts. **No before-and-after wall-clock was recorded**, which the plan asked for — the commits carry empty bodies and the live path is the only place that number exists. It stays a live-path row. | `api/review.py` · `_catalogue_search` |
| ✅ | ~~**The NOT CHECKED disclosure does not survive reopening a notice.** A review that skipped a candidate says so on screen, and the skip list lives only in component state — so a person coming back to the stored change requests sees "no viable part" with no sign that one of the named candidates was excluded before it was evaluated.~~ — P5, 11 Sep. `candidates_for` now reports every candidate that reached `consider` and resolved to nothing or came back ambiguous, with the sentence that stopped it; the run says each aloud in the preamble and writes them to `notices.review_skipped`; and `changes.tsx` populates the panel from that stored list, so it survives navigation and reload like the requests do. A catalogue hit the search itself filtered stays out of it, because that is the search shaping its shortlist rather than a skip. | `api/review.py` · `api/store.py` · `routes/changes.tsx` |
| ✅ | ~~`annual_volume` is a parameter of the review rather than a property of the product line, so recurring cost can be wrong in only one place.~~ — P3, 11 Sep. `OperatingProfile` now stores annual volume and its source; the seeded Sensor node, Gateway and Cabinet controller state 12,000, 20,000 and 4,800 respectively from the 2026 production plan. Both review paths read that profile, and neither request body accepts an unused annual-volume override. | `profile.py` · `seed_world.py` · `api/review.py` · `api/notices.py` |
| ✅ | ~~**A successful precedent is written, shown, and never read by a review.**~~ — P4, 11 Sep. An approved EOL substitution now records a refdes-independent `eol|{retiring}` signature; the next review of that retirement reads cross-line successes before the approved list and says which line resolved it. Duplicate approved entries remain one candidate, while rejections stay scoped to the board that measured them. | `api/review.py` · `review.py` · `api/store.py` |
| ✅ | ~~`backend/.env` still points `DATABASE_URL` at production, so a local run started without an override writes real rows.~~ — 11 Sep. The local file now names `postgresql:///continuity_demo`, and `.env.example` carries the same line with the reason, so the local database is the documented default and production is the URL you name explicitly rather than the one you fall into. The deployed instance is unaffected: `env.load` lets an injected platform variable win over the file. **The row's hazard and its remediation are different things** — while scoping this, a broad grep over `.env` printed the model key, the Neon password and the mailbox password into a session transcript, which is the exact anti-pattern `common/security.md` names. All three need rotating. | `backend/.env` · `backend/.env.example` · `OPERATING.md` |
| ✅ | ~~**Both board pictures came up blank after toggling away from BOARD and back.**~~ — 10 Sep, found while checking the placement cache above and not previously known. `Crop` created the SVG's blob URL in a `useMemo` and revoked it in a `useEffect` cleanup, and those have different lifecycles: React runs an effect, cleans it up and runs it again on mount under `StrictMode`, while a memo runs once — so the cleanup revoked the only URL there was and the `<image>` pointed at a blob the browser no longer had. `net::ERR_FILE_NOT_FOUND` in the console and **nothing on screen saying anything was wrong**: the caption, the crop border and our own violet overlay all still drew, so it looked like a board with nothing on it. `./demo.sh` serves the UI through Vite with `StrictMode` on, so this was the demo rather than a development-only curiosity. Creation and revocation now happen in one effect, so they always pair up. The browser test toggles away and back and **fetches the picture's own URL**, because every mark the test used to check was one we draw ourselves and was still there; reverting the fix turns it red. | `board/BoardConsequence.tsx` · `e2e/notice-arrival.spec.ts` |
| ✅ | ~~**`BoardConsequence` re-places the part on every toggle to BOARD**, because `auto` fires whenever `check` changes identity and nothing caches the result. A KiCad run is several seconds and the answer for a given (line, retiring, candidate) does not change.~~ — 10 Sep, and it was not only slow. The picture is replaced by **PLACING…** while a run happens, so a poll landing behind the BOARD view *takes the board away from whoever is looking at it* — which is how it was found: the browser test failed on a caption that had been on screen a moment earlier. `board/placements.ts` remembers each placement for the session, bounded at 24 and keyed case-insensitively on the three things that decide one, and the component paints from it on mount rather than starting a run. The same pass raised Playwright's assertion timeout from its five-second default to fifteen: every assertion in that spec waits on something that arrives over the network, and at five seconds it failed about one run in three, in a different place each time. **Three clean runs after, each on a rebuilt world.** The spec now says in a comment that it needs one, because it asserts `End of life (0)` before delivering the notice and a second run against the same world fails on its own precondition. | `board/placements.ts` · `playwright.config.ts` |
| ✅ | ~~**The substitution matrix is a form rather than a destination.** It is the only screen showing every candidate against every line including the losers, but `routes/matrix.tsx:148,257` asks the reader to tick the lines, type the refdes and type the candidate MPNs, all of which the review already knows.~~ — 10 Sep, [R6](RESEARCH-3rd-Passthrough.md#r6-what-the-substitution-matrix-is-for)'s own recommendation. `/changes` now carries **SHOW THE WORKING · 3 BOARDS AT U1** beside the change requests, linking into `/matrix` with the boards, the position and every part the review tried in the query string; the matrix reads them and runs itself, because opening a destination onto a filled-in form with a button still to press is the same form it replaced. One reference designator and **only the boards that carry it**, since the endpoint refuses a line that does not — a link naming U1 must not carry a board whose regulator is U7 and open on an error. The link is absent rather than dead when there is nothing to open it on. Verified in the browser: the grid comes up on the three affected boards with NCP1117 failing the Gateway on thermal to DESIGN and TLV1117 failing the cabinet on voltage and the Gateway on availability to PROCUREMENT. | `routes/matrixLink.ts` · `routes/matrix.tsx` · `routes/changes.tsx` |
| ✅ | ~~**A retired part is nearly indistinguishable in the memory graph.** `routes/memory.tsx:427` fills every part with the same orange whatever its lifecycle, and the whole encoding is a 2 px pale ring that vanishes at the zoom the graph settles at. Edges are worse: `:466-468` dashes only *historical* links, so the three edges joining AMS1117-3.3 to the boards still carrying it are drawn exactly like healthy ones.~~ — 10 Sep, all four of [R7](RESEARCH-3rd-Passthrough.md#r7-a-retired-part-should-look-retired-in-the-memory-graph)'s steps. The **fill** carries the lifecycle now, desaturated and darker so it separates in lightness as well as hue; the ring is back to meaning one thing and is heavier, because line width is the channel that survives greyscale and a projector. `NRND` or `OBSOLETE` sits under the part number, so the colour is never the only carrier and a screenshot travels without the legend. The **two kinds of gone** are now different edges: dashed grey for *was on this board and was replaced*, dashed warm and heavier for *is on this board and going away*. And the graph has a **legend** for the first time, four rows, where every encoding on it used to be folklore. Nothing new was encoded — shape still means kind and area still means uses — because adding a channel without removing one is how these pictures become unreadable. The decisions live in `routes/memoryGraph.ts` with five tests; the canvas only draws them. Verified in the browser on the seeded world: AMS1117-3.3 reads as retired at rest, and its three edges are the three most distinguishable things on screen rather than the least. | `routes/memoryGraph.ts` |
| ✅ | ~~**A candidate can still be excluded for ambiguity where the answer is knowable.** The BOM disambiguates a *fitted* part, but a candidate somebody names has no such record — so `TLV1117LV33DCYR`, which PARTS.md already resolves to TI's listing, is skipped rather than checked.~~ — 10 Sep. There was a second record all along: the approved manufacturer list says whose part quality qualified. `store.recorded_manufacturers` reads both, the bill winning where they disagree, and all three resolution sites now ask with it — the review's candidates, `/matrix`, and the notice-driven matrix, where `/matrix` had not been passing a manufacturer even for parts on the bill. Verified live on the seeded world: the matrix returns **no ambiguous and no unresolved candidates**, `TLV1117LV33DCYR` is checked as Texas Instruments on all three boards, and it is TI's 5.5 V ceiling that fails the 12 V cabinet controller — the finding the skip was hiding. What is still open is a caller naming a manufacturer for a part this company has never bought or qualified, which needs a field on the request. | `api/matrix.py` · `api/notices.py` |
| ✅ | ~~**A product line can carry the same notice twice**, once seeded and once mailed, and the page renders two identical banners with two REVIEW THIS LINE buttons. Nothing dedupes on `(org, mpn, effective_date)`.~~ — 10 Sep. `save_notice` now returns the id it already holds for `(org, mpn, effective_date, replacement_mpn)`. The replacement is in the key because those three fields are what make it a *different* notice to act on, and they are exactly what separates the preliminary `PCN-2026-118` from the full `PCN-2026-114` — same part, no date, no recommendation — which must stay two notices and now have a test saying so. `IS NOT DISTINCT FROM` rather than `=`, because a notice naming no date has NULL there and NULL never equals NULL, which would have made the preliminary one new on every arrival. The first arrival stays the record, `source` included: how a change first reached us is a fact about the change. Verified live: posting `PCN-2026-114.pdf` twice returns one id, leaves one stored notice, and still answers all three affected products on the second call. | `api/store.py` · `save_notice` |
| ✅ | ~~**The offline safety net does not cover reading a notice.** `CONTINUITY_FIXTURES=1` replays every distributor call and never touches the network, and part normalisation comes out of `backend/cache/`, so the whole review path runs on a dead connection. The notice reader calls the model directly with no recorded path, so the one step that must work first is the one step that needs the venue's wifi.~~ — 10 Sep. `notices.read` now records and replays through the same `fixtures` module every distributor call uses, keyed on a digest of the extracted text **and** a fingerprint of the instructions, because a recording made under one prompt is not an answer to a different one. Both committed notices are recorded, so the demo's first move replays. Measured on `PCN-2026-114.pdf`: **1903 ms live, 7 ms replayed, identical reading.** The verification still runs on the replayed reply — a recording is the model's answer, not a licence to believe it — so a quoted line that is not in this document is refused either way. A notice nobody has recorded is **refused rather than fetched**, which is the contract the rest of the net keeps, and `./demo.sh --live` records a new one. Closing this exposed that a suite run had been writing its stub payloads into the committed recordings; `conftest.scratch_recordings` now redirects every write the suite makes. | `notices.py` · `parts/fixtures.py` · `tests/conftest.py` |
| ✅ | ~~**Applying a decision writes the distributor's manufacturer, not the datasheet's.** The Gateway's bill read `TLV1117LV33DCYR · JSMSEMI`, because that is the manufacturer on the JLCPCB listing the candidate was resolved from; the part is Texas Instruments' and the whole `ENGINEERING_FIELDS` argument rests on TI's document being the specification of record.~~ — 10 Sep, and it was worse than the row said. Two lookups identified a part by number alone. `candidates_for` resolved every approved part with `resolve(mpn)`, so a part this company had qualified as TI's came back as whichever listing the distributor returned first; it now asks with `store.recorded_manufacturers`, which is the company's own bills and then its approved list. `search.live_stock` matched rows on the `model` field alone, so the **availability verdict that gates procurement in the demo was computed from another company's shelf** — the recording holds JSMSEMI at 166,924 and TI at 1,133 for that one number, and the run reported JSMSEMI's part with TI's stock. It now matches the manufacturer too, and answers `None` rather than somebody else's figure when no row is that company's. Neither change alters a recorded call, so all 617 fixtures stay valid. Verified live end to end: the Gateway's bill now reads `TLV1117LV33DCYR · Texas Instruments`, the shortfall is still 1,133 against 5,000, and the three products still answer differently. | `review.py` · `parts/search.py` · `api/store.py` |
| ✅ | ~~**The EOL flow has no waiver, and the design flow has a good one.** `graph/state.py:95-102` records what a person waived as `(rule, subject, mpn, revision)`, scoped so that changing the part or the revision evaporates it, and `validate` marks the verdict **accepted rather than passed** — *"a waiver is not a pass"*. `api/review.answer_decision` has nothing equivalent: it approves or declines a whole decision.~~ — 10 Sep. An approved decision carrying a `gate_rule` now *is* the waiver. `store.accepted_waivers_for_line` reads it back as `(rule, subject, mpn, revision, roles)` from the approval the owning desk signed, and `review.accepted_verdicts` marks the matching failure `accepted` without touching its status, detail or evidence — so `rules.blocking` stops routing it and the screen keeps reporting it. Both readers of a board share that one function: `POST /lines/{id}/check` for the resting statement, and `review.attempt` for a later change that re-checks the same board. Scoping is unit-tested five ways, each of which leaves the failure blocking: a revision the board has left, a different candidate, a different slot, a different rule, and a desk that does not own the rule. Verified live on the demo world: four desks signed the Gateway, the bill moved to `TLV1117LV33DCYR` at Rev D, and re-checking reported `u1` **accepted · availability** with *1,133 in stock at JLCPCB, below the 5,000 minimum* intact — neither a conflict nor a pass. The workspace says so too: U1 is amber and dashed with a legend row that appears only when a board carries one, and the resting statement now survives the stored trace that predates the signature, because that trace can never be told about it without rewriting what the run said. | `api/review.py` · `review.py` · `api/store.py` · `review/ReviewTrace.tsx` |
| ✅ | ~~**The lanes drop every `candidate` and `check` frame.** `api/review.py:361,375` emits both per line with a `line_id`, and `ReviewLanes` had no case for either, so an expanded lane showed narration and never the twenty-two rule verdicts that are the argument.~~ — 10 Sep. The lane reducer now keeps candidate attempts as narration and every check’s status, evidence detail, margin and ownership in the order the stream emitted them. Collapsed rows remain one-line summaries; expansion renders the candidate sequence and verdict trace with an explicit verdict word, never colour alone. Playwright delivered `PCN-2026-114`, ran the company review, expanded Sensor node, and observed candidate attempts plus `SATISFIED · thermal dissipation` and the rest of the trace. The unit test was RED/GREEN/perturbation-verified. [R2](RESEARCH-3rd-Passthrough.md#r2-every-lane-should-stream-its-own-trace) | `review/ReviewLanes.tsx` · `e2e/notice-arrival.spec.ts` |
| ✅ | ~~**The before and after board pictures are indistinguishable and nothing encodes the difference.**~~ — 10 Sep. The AFTER crop now carries a neutral violet, crop-coordinate-anchored overlay and border; it communicates *changed*, never *safe*. The factual caption states the footprint and fitted-part identities, explicitly including `DROP-IN` where applicable. A focused unit test covers nonzero SVG crop geometry and treatment semantics; production build and Playwright on a fresh demo world passed, with the browser waiting for real KiCad completion and observing the caption and nonzero overlay position. | `board/BoardConsequence.tsx` · `e2e/notice-arrival.spec.ts` |
| ✅ | ~~**The board pane says *Drawing the board…***~~ — 10 Sep. It now says *Loading the board…*, which is the operation it performs. Covered by the same production build and board-evidence browser pass. | `review/BoardPane.tsx` |
| ✅ | ~~**A mailed notice announces itself nowhere.** `routes/changes.tsx:19,54` was the only poll in the product, so a notice read by the server changed `/changes` and nothing else: no notification, and `/lines` and `/lines/:id` never refetched.~~ — BUILD item 28, 10 Sep. `NoticeArrivalProvider` polls `GET /notices` once above authenticated routes, announces each new notice with its affected-product count, and increments one refresh signal that `/changes`, `/lines`, and open product lines consume. The toast is dismissible, not modal. Playwright on the freshly seeded local world delivered `PCN-2026-114` after both `/lines` and Sensor node had established their baseline; both browser pages showed **“AMS1117-3.3 affects 3 product lines.”**, the product row showed **1 notice**, and the open line changed **End of life (0) → (1)** without navigation. The test takes credentials only from runtime environment variables, never from source. | `hooks/useNoticeArrivals.tsx` · `routes/lines.tsx` · `routes/line.tsx` · `e2e/notice-arrival.spec.ts` |
| ✅ | ~~**Role-specific rendering is not built.** SCENARIO-B asks for one finding in three renderings, so each desk sees the same verdict in its own terms. Every finding renders identically for everybody today.~~ — Item 33, 10 Sep. `roles.by_department` is the one grouping function and the trace, the lanes, the change request and the matrix all read it. A rule with two owners renders under both, which answers RESEARCH-BRIEF-2's question about whether one finding can legitimately have several owners. | `roles.py` · `review/Departments.tsx` |
| ✅ | ~~**Two of the four desks can never be asked, even when their own rule is what failed.** `GATE_RULES = ("part_qualification", "source_approval")` at `review.py:37`, and everything else is `physical` — *"failures no signature can clear"*. So a candidate failing `availability`, which is procurement's entire job, is discarded rather than routed, and the same for `footprint`, which is production's. The distinction is right in principle and two rules too narrow: a junction temperature is a wall because no signature lowers it, a stock shortfall is a decision because procurement can bridge-buy or accept a lead time, and a package change is a decision whose answer is *production accepts a board revision*. **This blocks BUILD item 38**, which was planned on the assumption that a stock failure routes. [R8](RESEARCH-3rd-Passthrough.md#r8-the-cross-team-response-is-the-problem-statement-and-one-desk-answers-everything)~~ — Item 32, 10 Sep. `ANSWERABLE_RULES` is five rules wide and lives in `roles.py` beside the routing table. A test asserts arithmetic is still a wall. | `roles.py` · `review.py` |
| ✅ | ~~**Role-specific rendering of a shared finding was specified on 4 Sep and never built.** SCENARIO-B's own gap analysis lists it, and states the design: *"each role sees the same verdict in its own terms. One finding, three renderings — a view layer over one shared result, never three engines."* Nothing in the product groups a result by the desk that owns it. This is the answer to *how do we show the department breakdown*, and it has been sitting in the repository for six days. BUILD item 33. [R11](RESEARCH-3rd-Passthrough.md#r11-showing-four-desks-and-demoing-them-with-one-presenter)~~ — Item 33, 10 Sep. `roles.by_department` is the one grouping function, and the trace, the lanes, the change request and the matrix all read it. | `roles.py` · `review/Departments.tsx` |
| ✅ | ~~**The frontend receives the signed-in user's roles and no route uses them.** `PublicUser` carries `roles: string[]` (`lib/api.ts:26`), and the only place a role reaches the screen outside `/matrix` is a question's own `roles` field. Nothing says which desk you are, nothing marks a decision as yours or not yours, and a question addressed elsewhere renders with live buttons that will 403. [R11](RESEARCH-3rd-Passthrough.md#r11-showing-four-desks-and-demoing-them-with-one-presenter)~~ — Items 36 and 37, 10 Sep. `/approvals` says which desk you hold, a decision this desk has signed shows what it signed instead of buttons that would 409, and the rail carries the desk and a count of what it owes. | `shell/` · `routes/approvals.tsx` |
| ✅ | ~~**Every reference implementation of a multi-desk demo switches principal in the header; ours would have to sign out.** Two seeded accounts, no switcher, and SCENARIO-B's open question *"how this is demoed on stage in the time available"* was never answered. A *view-as* simulation is the wrong shape here because a desk has to actually sign, so this is several real sessions and one control, not an impersonation mode. BUILD item 37. [R11](RESEARCH-3rd-Passthrough.md#r11-showing-four-desks-and-demoing-them-with-one-presenter)~~ — Item 37, 10 Sep. A second httpOnly cookie holds every session this browser authenticated; `/auth/sessions` lists them without ever returning a token and `/auth/switch` makes one active. No path to a session the caller did not sign into. | `api/auth.py` · `shell/DeskSwitcher.tsx` |
| ✅ | ~~**The cross-team response is the problem statement, and one desk answers everything.** Scenario B asks *how does your tool coordinate the cross-team response — design validates alternatives, procurement checks availability, and production confirms assembly compatibility*. Every proposal, every question and every change request in the demo says **engineering**. `review.choose` (`review.py:152-162`) hardcodes `roles=("engineering",)` for any candidate that clears, and only its second pass, reached when *nothing* clears, calls `decision_roles`; `change._approvals_for` (`change.py:293-309`) falls back to engineering the same way. A department is therefore consulted only when the answer is a compromise. Grep the frontend and *procurement* and *production* appear once, in a comment. The engine does check all three departments' constraints on every candidate; nothing on screen ever says so. Attributing every rule to its desk **pass or fail** is presentation work over facts already computed, and is the first fix. [R8](RESEARCH-3rd-Passthrough.md#r8-the-cross-team-response-is-the-problem-statement-and-one-desk-answers-everything)~~ — Items 31 to 35, 10 Sep. `choose` and `_approvals_for` now ask `desks_that_must_sign`; every trace, lane and change request groups its verdicts by the desk that owns them; every substitution is signed by each department that examined it. | `review.py` · `change.py` · `review/` |
| ✅ | ~~**There is no production desk.** `api/store.py:71` is `ROLES = ("engineering", "procurement", "quality")`, so the third department the assigned scenario names cannot be granted, assigned or signed as. SCENARIO-B's own mapping gives production the `footprint` rule, and `roles.py:30-31` routes both `footprint` and `footprint_compatibility` to engineering. FLOW.md §8 already calls the board consequence *"production's leg of the scenario"*, and it is the one piece of production evidence the product computes, unattributed. Adding the role and moving two entries is small; `tests/test_roles.py` already asserts every rule is mapped, so a half-done change fails. [R8](RESEARCH-3rd-Passthrough.md#r8-the-cross-team-response-is-the-problem-statement-and-one-desk-answers-everything)~~ — Item 31, 10 Sep. `ROLES` carries it, `footprint` and `footprint_compatibility` route to it, the seed creates a production account, and a test asserts every department the routing table names is one the product can grant. | `api/store.py` · `roles.py` |
| ✅ | ~~**Procurement's rule cannot fail in the seeded world.** `availability` fails below `requirements.min_stock`, which defaults to 100 (`engine/models.py:468`), and every candidate has thousands at JLCPCB. A lifecycle concern reports as *satisfied* with a margin note rather than as a failure, which is why the Sensor node card shows availability in green. Putting a real volume on one product line's operating profile — TLV1117LV33DCYR has 1,133 in stock, and a gateway shipping 5,000 a quarter needs 5,000 — is a genuine availability failure that routes to procurement with no code change. Production's rule is unreachable for the mirror-image reason: `_search_query` only sources parts in the retiring part's own package, so no candidate can fail `footprint`. [R8](RESEARCH-3rd-Passthrough.md#r8-the-cross-team-response-is-the-problem-statement-and-one-desk-answers-everything)~~ — Item 38, 10 Sep. The Gateway states a build quantity of 5,000 a quarter on its operating profile, so TLV1117's 1,133 in stock is short of it and that line waits on procurement. Production's rule stays unreachable, because the catalogue is searched in the retiring part's own package. | `tools/seed_world.py` · `profile.py` |
| ✅ | ~~**The 48 hours the scenario hands us appears nowhere in the product.** The topic states the clock, SCENARIO-B's rubric note says Efficiency Gains wants a quantified before and after, and no surface carries a time figure of any kind: not the change request, not the lanes, not `/memory`. The honest version is not a fabricated saving but the count of round trips removed, which is derivable from what already ran — one notice, three boards, four candidates, twenty-two rules each, checked before anybody was asked anything.~~ — Item 39, 10 Sep. The change request counts what was checked before anybody was asked. Not a saving: nobody measured one. | `change.py` · `review/RequestCard.tsx` |
| ✅ | ~~**An engineer can qualify a part on their own, and the routing table says they should not be able to.** `roles.py` addresses `part_qualification` to engineering *and* quality with the words *"it needs both: engineering says the part is right, quality says it is allowed"*, and the code reads that list as *any one of these may answer*. So the strongest beat in the scenario — the decision that is not yours — is only true for `source_approval`, which is procurement's alone. Faithful would be two signatures on one decision, which the `decisions` row cannot express today: it has one state and one `decided_by`. Found by a test that expected a 403 and got a 200. **Becomes 🔴 the moment the routing above is fixed**, because it stops being latent and fires on stage in front of the person asking how the cross-team response is coordinated.~~ — Item 35, 10 Sep. `answer_decision` records a signature per desk and applies only when the last one signs, so `part_qualification` finally needs both. The `approvals` table already had `decision_id`; no migration. | `roles.py` · `api/review.py` |
| ✅ | ~~**Nothing tells the desk that a decision is waiting for it.** The run addresses a question to quality or procurement and records it, and the only way that desk finds out is by opening the app. This is the outbound half of the mail work, and it is what makes the routing a workflow rather than a label.~~ — Item 36, 10 Sep. `GET /decisions` is the desk's own queue across every product line, `/approvals` renders it, and the rail carries the count. | `api/decisions.py` · `routes/approvals.tsx` |
| ✅ | ~~The demo's own recordings are not all committed, and replay is now the default.~~ Measured 10 Sep: with the working tree reduced to exactly what git tracks, the review died on its second frame, because the `jlc_search` for the incumbent was one of 43 recordings that existed only on this laptop. All 617 are committed as of `b18b738`, so a fresh clone runs the demo. 10 Sep | `backend/fixtures/` |
| ✅ | ~~A finished review's trace is lost on navigation~~ — see *a review's trace is not persisted*, resolved the same way. 9 Sep | `routes/line.tsx` |
| ✅ | ~~A successful precedent is never written~~ — `store.record_precedents` is called the moment a substitution is approved rather than merely proposed, and `/memory` shows it. It is still not *read* by a review, which is its own row above. 8 Sep | `api/review.py` |
| ✅ | ~~Approvals are recorded and never shown~~ — `/memory` reads them under **WHAT WAS DECIDED**, with the margin and who signed. 8 Sep | `api/recall.py` |
| ✅ | ~~A question shows no sign of whose it is~~ — *engineering decides* sits above the buttons in both places a question is answered, so the desk is named before somebody presses approve rather than by a 403 afterwards. 9 Sep | `review/ReviewLanes.tsx` · `review/ReviewTrace.tsx` |
| ✅ | ~~The matrix cannot be built from stored data alone without a hand-written profile~~ — `tools/seed_world.py` writes rail `source` and `members` with every profile, so `board_from` has what it needs from the database alone. 8 Sep | `tools/seed_world.py` |
| ✅ | ~~A product line has no page~~ — clicking a row went to `/design/:id` and greeted a shipping product with *"What are you building?"*. `routes/line.tsx` is that page, and since item 25 it is the design workspace pointed at a product that already exists. 9 Sep | `routes/line.tsx` |
| ✅ | ~~A shipping product reads "Never run"~~ — the row's status came from the latest *thread*, so a product on its third revision was labelled by whether somebody had designed it from a brief inside our tool. It reads its exposure and its revision now. 8 Sep | `routes/lines.tsx` |
| ✅ | ~~The new screens do not share the page chrome~~ — `shell/Page.tsx` is the one definition, and `/changes` and `/matrix` wear it. `/lines/:id` deliberately does not: it is a viewport-locked workspace, and `shell/workspace.ts` owns that answer for both shells. 8 Sep | `shell/Page.tsx` · `shell/workspace.ts` |
| ✅ | ~~There is no one place the demo happens~~ — there are two, and which is which is the whole navigation: `/changes` is the company view and `/lines/:id` is one product. The same endpoint runs both, narrowed by `line_id`. 9 Sep | `routes/changes.tsx` · `routes/line.tsx` |
| ✅ | ~~The review does not reason and never asks~~ — item 20. `POST /notices/{id}/review/run` streams reasoning, candidates, checks and a question addressed to a desk, and `POST /decisions/{id}` answers it. The one-shot endpoint that computed finished documents still exists and is unreachable from the app, which is its own row above. 8 Sep | `api/review.py` |
| ✅ | ~~Nothing is ever applied~~ — item 21. Approving writes the part into the bill, moves the revision, records who signed and why, and writes the successful precedent. Verified on three product lines. 8 Sep | `api/review.py` |
| ✅ | ~~Memory is the 1.0 model untouched~~ — item 23. `api/recall.py` assembles the company's record from lines, bills, notices, precedents, approvals and decisions; threads are one source among several rather than the only one. 8 Sep | `api/recall.py` |
| ✅ | ~~The walkthrough teaches the old story~~ — item 24. The route is deleted. Its replay machinery is still inside `sseClient`, which is its own row above. 8 Sep | `routes/walkthrough.tsx` |
| ✅ | ~~RUNNER.md step 4 documents an answer the build no longer gives~~ — rewritten twice since, most recently on 10 Sep against a freshly seeded world walked end to end. The three answers in it are the three the run actually produces. 10 Sep | `docs/world-finals/RUNNER.md` |
| ✅ | ~~The KiCad render does not highlight the retired part~~ — both pictures crop to the same rectangle around the part and draw a dashed ring on it, in board millimetres, which is why the render is asked for in page coordinates rather than fitted to the board. 9 Sep | `board/BoardConsequence.tsx` · `board/BoardPicture.tsx` |
| ✅ | ~~Every visit to a product line resolves its parts again~~ — measured at 4.1 s a visit, every visit. Keyed on a digest of the bill, the profile and the revision, and every line is checked once at startup, so the first person to open one is not the person who waits. 4.1 s → 11 ms. 10 Sep | `api/lines.py` |
| ✅ | ~~A review's trace is not persisted, so a reload loses it~~ — item 30a. `api/replay.frames_from` rebuilds the trace out of `decisions.document`, which the run already wrote, and it goes through the same reducer a live run goes through. Nothing new is stored. 9 Sep | `api/replay.py` |
| ✅ | ~~`/lines/{id}/check` returned 500 on every seeded product line~~ — so the page a demo opens with was painted entirely by its fallbacks, and the browser reported it only as a CORS error because an unhandled exception escapes outside the CORS middleware. `capacitance_uf` was added to `DOSSIER_FIELDS` on 9 Sep and deliberately kept out of `_FLOAT_FIELDS` — on the entirely correct grounds that a distributor states an MLCC's value accurately and a stored figure should not outrank a listing. That is an argument about *precedence*, which is `ENGINEERING_FIELDS`, and none at all about *type*. A recorded 22 µF came back as `"22.0"`, reached `PartSpec.capacitance_uf`, and `capacitor_requirements` summed a str into an int. The test written with the field checked only the write direction, so it passed throughout. Two tests now cover the round trip, one of them derived from `PartSpec`'s own annotations rather than a hand-kept list, because a hand-kept list is the thing that was wrong. 9 Sep | `parts/dossier.py` |
| ✅ | ~~Reseeding the demo world failed once it had actually been used~~ — `tools/seed_world.py --reset` deleted the users and let everything cascade off them, which held only while nobody had answered anything. `decisions.decided_by` is ON DELETE SET NULL and `decisions.line_id` cascades, so deleting a user made PostgreSQL update a decision row whose product line the same statement had already removed, and the update re-checked a foreign key with nothing left to point at. Whether it fires depends on the order rows come off disk, so a fresh test database never saw it and the demo database hit it every time after a run ending in an approval. The reset now deletes the lines while their owners still exist, so the decisions go outright and the ordering question does not arise. Reproduced and fixed against a `pg_dump` copy of the failing world. 8 Sep | `tools/seed_world.py` |
| ✅ | ~~A notice reviewed twice listed every product line twice~~ — the listing returned every change request ever written for the notice rather than the current one per line, so a second review with one more candidate produced a screen showing each product line's answer alongside its own previous answer. Every document is still kept, because a change request is a record of what was decided; the listing is now the latest per line. Found in the browser while verifying item 18. 8 Sep | `api/store.py` |
| ✅ | ~~The reader took the last time *ship* date as the date ordering ends~~ — found by building a real notice for item 17. `PCN-2026-114` carries four dates, and against the live model the reader returned **2027-09-30** rather than the last time buy of 2027-03-31, quoted honestly from a real line and passing every check we had: six months of runway the notice does not give. The instruction now names which date is wanted and which are decoys, and removing it reproduces the wrong answer on demand. 8 Sep | `continuity/notices.py` |
| ✅ | ~~A word where a part number belongs was believed to be a part number~~ — a preliminary notice writes *Recommended replacement: none.* The line is real, so quoting it proved nothing, and `none` passed the shape test: the review would have gone looking for a part by that name. A recommendation is now refused when it is a word notices use for absence, and — the rule the affected part already lived under — unless the line quoted for it actually prints it. Both perturbation-verified. 8 Sep | `continuity/notices.py` |
| ✅ | ~~A review's NOT CHECKED panel carried across to the next notice~~ — uploading a second notice cleared the change requests and left the panel standing, so a candidate excluded from the *previous* review read as a finding about the document now on screen. Found in the browser, uploading `PCN-2026-118` behind `PCN-2026-114`. 8 Sep | `routes/notices.tsx` |
| ✅ | ~~The change request had no screen~~ — the document the whole product exists to produce lived only as JSON on an endpoint. `routes/notices.tsx` renders it: proposal, every rejection with the sentence that killed it, evidence, what was not assessed *and* what could not be checked, cost split, and the desks that have to sign. Verified in a browser on the seeded world, from uploading a real PDF to three finished requests. 8 Sep | `routes/notices.tsx` |
| ✅ | ~~The ambiguity guard refused the incumbent, so no board could be assembled~~ — JLCPCB lists AMS1117-3.3 under three manufacturers, and a part already on a bill of materials is not ambiguous: the row says whose it is. Found by running the seeded world through the browser, where the whole review returned "no affected line could be assembled". 8 Sep | `api/matrix.py` |
| ✅ | ~~Nothing wrote a verified part fact, so the rule that a datasheet outranks a listing could never fire~~ — `tools/seed_world.py` is the writer it was built for. Four SOT-223 regulators sourced live come back with one shared package-table figure and no junction limit; the seeded readings tell them apart. 8 Sep | `tools/seed_world.py` |
| ✅ | ~~Joining a company left the one-person organisation behind, empty and owned by nobody~~ — one per join, invisible to any query anyone would think to run. Found by the seed leaving three behind for two people. `_drop_if_vacated` checks every org-scoped table explicitly, because most of them cascade and a wrong guess would delete the work rather than be refused. 8 Sep | `api/store.py` |
| ✅ | ~~`margin` had never been observed travelling end to end on a real run~~ — the field serialised and a unit test covered it, but no live board had produced a satisfied-and-narrow check. Item 12's matrix does: LD1117 holds the gateway by **1.5 °C**, rendered in its own colour beside three parts that pass outright, seen in a browser. 7 Sep | `routes/matrix.tsx` |
| ✅ | ~~The dashboard row truncated the ambient's source~~ — the line's facts now live on the matrix, where each cell's evidence rows carry every number with where it came from. 7 Sep | `routes/matrix.tsx` |
| ✅ | ~~`tools/brief_sweep.py` detected nothing of the class it was built for~~ — both headline detectors filtered `status == "fail"` and returned `[]` on every run, while the file's own comment called that check "the headline bug this project exists to prevent". The thin-data flag now counts `evidence_missing` only: `not_assessed` is a boundary declared on every board, so it would add a constant to a threshold, and `not_applicable` is an answer rather than a gap. `tests/test_brief_sweep.py` pins the spellings to `CheckStatus`, and was confirmed to fail when the old one is put back. 7 Sep | `tools/brief_sweep.py` |
| ✅ | ~~Test data left in the production database~~ — the `labels-check@example.com` account, 4 lines, 6 threads, 2 findings and 439 run_events, deleted 7 Sep through the `users` cascade after a full local backup. Zero orphaned rows afterwards. Deleting it also made the replay bug's true scale visible: with the test data gone, **every** check event left in production is in the retired vocabulary — all 5 threads, 216 events — so before that fix, every run a judge could open on the deployed app captioned "No checks yet." | Neon `neondb` |
| ✅ | ~~The recorded walkthrough was entirely pre-rename~~ — 33 `pass`, 3 `warn`, 3 `fail`, no margins, and three checks shown as NO EVIDENCE that the engine had performed correctly. Re-recorded through `tools/record_walkthrough.py`, which drives the real HTTP route against the offline catalogue: 33 satisfied, 3 failed, 3 evidence_missing, 4 not_applicable, 3 not_assessed, one margin, and the same three-beat story. 7 Sep | `api/walkthrough.jsonl` |
| ✅ | ~~Stored runs replayed in the retired vocabulary~~ — the mapping lived inside `walkthrough_frames()` only. `events.with_current_labels` is now the one mechanism, applied at both reads. Verified against a real 117-frame production trace: all 58 check events translated, every other frame byte-identical. 7 Sep | `api/events.py` |
| ✅ | ~~A waived failure was relabelled `evidence_missing`~~ — the only thing the three-label vocabulary could reach for, and it said the opposite of the truth about a finding whose evidence the user had just read. `Verdict.accepted` is now an attribute of `failed`, exactly as `margin` is an attribute of `satisfied`; `rules.blocking()` is what the graph routes on while `rules.failures()` still answers what failed. This also deleted the second mechanism: `api/memory.py` matched the acceptance *narration* with a regular expression, which `acceptance_message`'s own docstring said to remove as soon as a structured signal existed. 7 Sep | `engine/models.py` · `graph/nodes.py` · `api/memory.py` |
| ✅ | ~~Board-scoped verdicts rendered nowhere~~ — `BOARD_SUBJECT` gave coverage boundaries a subject the UI's slot-grouping could not represent, so they were collected into a map entry nothing read. The trace now names them once when a run ends — *"Not assessed: output capacitor stability, EMC, signal integrity. 4 rules did not apply to this board."* — which is the opposite of the bug it replaced, where every sourcing line was captioned "3 not assessed" as though each part were at fault. 7 Sep | `ChatPanel.tsx` |
| ✅ | ~~The runnable demo matrix reported nothing on every cell~~ — `tools/eol_differential` counted `pass`, `warn` and `fail` after item 3a retired all three, so twelve cells printed "0 pass, 0 FAIL, 0 warn" while the engine underneath was answering correctly. The label list is now checked against `CheckStatus` itself, 7 Sep | `tools/eol_differential.py` |
| ✅ | ~~A thermal margin was rounded to whole degrees~~ — the gateway holds by 1.46 °C and printed "1 °C"; a board holding by four tenths would have printed "0 °C", which reads as no headroom at all, 7 Sep | `engine/format.py` |
| ✅ | ~~`api/memory.py` recorded a repair as successful when the check still failed~~ — the guard compared against `"fail"`, a spelling the five labels retired, so it matched nothing. Now confirms only on `satisfied`, 7 Sep | `api/memory.py` |
| ✅ | ~~Board-wide verdicts blamed on whichever part was placed first~~ — `BOARD_SUBJECT`, 7 Sep | `engine/rules.py` |
| ✅ | ~~A package-table entry stopped the engine fetching a published θJA~~ — the gate treated "we have an approximation" as "this is answered", 7 Sep | `graph/sourcing.py` |
| ✅ | ~~The ambient was a silent default of 25 °C~~ — `ambient_source`, 7 Sep | `engine/models.py` |
